import Foundation

/// バージョンごとに出す・出さないを切り替える札。
///
/// **消すのではなく、出さないだけにする。** 消すと戻すときに作り直しになり、
/// 別のセッションが同じところを触っていると衝突する。
enum AppFeature {

    /// 投げ銭を出すか。
    ///
    /// **1.0 では出さなかった。** 4.3(a)（既存のアプリと区別が付かない）で却下された直後で、
    /// 再審査の論点を「このアプリは独自か」の一点に絞りたかった。
    /// **1.1 で戻す。** 課金は App Store Connect 側も `READY_TO_SUBMIT` にしてある。
    static let showsTipJar = true
}
