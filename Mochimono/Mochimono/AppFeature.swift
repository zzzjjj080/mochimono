import Foundation

/// バージョンごとに出す・出さないを切り替える札。
///
/// **消すのではなく、出さないだけにする。** 消すと戻すときに作り直しになり、
/// 別のセッションが同じところを触っていると衝突する。
enum AppFeature {

    /// 投げ銭を出すか。
    ///
    /// **1.0 では出さない。** 4.3(a)（既存のアプリと区別が付かない）で却下された直後で、
    /// 再審査の論点を「このアプリは独自か」の一点に絞りたい。
    /// 課金を足すと、審査メモの書き直しと課金そのものの審査が論点に加わる。
    /// **1.0 が通ったら 1.1 で true に戻す。**
    static let showsTipJar = false
}
