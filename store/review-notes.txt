No account, login, or credentials are required. No subscriptions, no
user-generated content shared with anyone, and no advertising or analytics.

This version adds ONE consumable in-app purchase: a voluntary tip
("com.zzzjjj080.Mochimono.coffee"). It unlocks nothing. Nothing in the app is
gated, hidden, or changed by purchasing it. The app shows a thank-you message
and a count of tips sent from this device. The purchase row is in Settings.

WHAT THE APP DOES

Tilecheck turns plain text into a colour-coded board.

There is no "add item" form. The user types lines in a plain text editor, one
item per line. A blank line is the only piece of syntax: it starts a new group,
and each group is automatically given a different colour. Groups have no names -
the separator is a blank line and the distinction is colour. The result is shown
as a grid of tiles rather than a vertical list, so the whole set is visible at
once. Tapping a tile marks it done. Checks stay until the user clears them, so
the same board is reused.

The board can be turned back into plain text and shared at any time, and text
from any other app can be pasted in to become a board. There is no proprietary
format.

WHAT IS NEW IN THIS VERSION

- "残りだけ" (remaining only): hides tiles already marked done, so only what is
  still missing is shown.
- Duplicating a board: swipe a row right on the first screen. The text, columns
  and colour scheme are copied; checks and the completion date are not.
- The date a board was last completed is shown on the first screen.
- The voluntary tip described above.

HOW TO EXERCISE THE APP

1. On launch, three boards are already present. Tap any of them.
2. Tap any tile. It turns a stronger colour and the counter at the top right
   increases. Tap again to undo.
3. Tap "残りだけ" above the tiles. Tiles already marked done disappear; tap it
   again to show them. This is a view setting only and is not saved.
4. Tap the left/right triangles above the tiles to cycle the colour scheme
   (12 schemes, the hues rotate one step at a time). Each board keeps its own.
5. Tap "編集" (Edit) at the bottom right to see the plain text this board is made
   of. Adding a blank line splits the items into another colour group. Existing
   checks are preserved by item name when the text is edited.
6. Tap "設定" (Settings) in the navigation bar, then "テキストで書き出す"
   (Export as text) to open the share sheet. What is shared is ordinary text.
   The tip row ("開発者にコーヒーを奢る") is on the same screen.
7. Go back to the first screen and tap "+". The first section is "テキストから作る"
   (Create from text) with a paste button. Paste any bulleted text and it becomes
   a board.
8. Swipe a row right on the first screen to duplicate it; swipe left to delete
   it. Deleting shows a confirmation alert naming the board and how many items
   will be lost.

DATA AND PRIVACY

All data is stored on the device using UserDefaults. Nothing the user writes is
transmitted. The app does not use any third-party SDK, analytics, or
advertising. The only network use is StoreKit, to show the tip price and to
process the tip; that traffic goes to Apple. All source code was written by us
for this app; no template or purchased source was used.

External services: none. Only Apple frameworks (SwiftUI, StoreKit, UIKit for
haptics).
Regional differences: none. The app is Japanese only and is distributed in Japan.
The app name is in Latin script but the interface is entirely in Japanese.
Not a medical app. No diagnosis, treatment, or health claims.
Accessibility: the app follows the system text size setting and reduces the
number of columns at accessibility sizes. Colour contrast between each tile and
its label is verified to meet WCAG AA (4.5:1) by automated tests for every colour
scheme in both light and dark appearance.
